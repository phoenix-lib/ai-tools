---
phase: 5
plan: "05-02"
type: implementation
wave: 2
depends_on:
  - "05-01"
files_modified:
  - tools/contract-drift-auditor/discovery.js
  - tools/contract-drift-auditor/references.js
  - test/contract-drift-auditor/discovery.test.js
  - test/contract-drift-auditor/integration.test.js
  - docs/RELEASE-READINESS.md
  - CHANGELOG.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04
  - GATE-03
---

# Plan 05-02: Harden Self-Audit and Run Release Gate Review

## Gate Resolution

- **Research Gate:** user selected `Research first`; evidence in `05-RESEARCH.md`.
- **Upstream Freshness Gate:** passed before planning at
  `2079ab9626e9f9ed256512091f9c5ea473582885`.
- **Cross-Repo Incoming Review Gate:** existing changelog/gate-review request
  remains planned; this plan executes the release/maintenance review portion.
- **Cross-Repo Outgoing Need Gate:** not applicable unless execution finds a
  missing kit-owned protocol requirement. If that happens, create an outbox
  request and record a decision instead of implementing kit-owned behavior.
- **AI Tools Self-Use Gate:** applies and must run. The validated
  `contract-drift-auditor` should be run against this repository with `--out`
  outside the target after source filtering is hardened.
- **New Tool Intake Gate:** no new v1 tool is promoted. `XREPO-VALIDATOR-01`
  and `GATELINT-01` remain v2 seeds.
- **Future Gate Review Hook:** applies. Use current `ai-workspace-kit`
  gate-review guidance manually; any future mechanical output is evidence only.
- **Project Changelog Gate:** update `CHANGELOG.md` because this plan changes
  auditor behavior and release readiness evidence.

<objective>
Make release readiness evidence trustworthy by reducing full-repo self-audit
noise, running the validated auditor against AI Tools itself, and recording a
manual gate review using current `ai-workspace-kit` guidance without adding
cross-repo automation.
</objective>

<context>
- Depends on plan `05-01` docs and release readiness checklist.
- Phase context: `.planning/phases/05-integration-and-release-hardening/05-CONTEXT.md`
- Research: `.planning/phases/05-integration-and-release-hardening/05-RESEARCH.md`
- Phase 4 self-use evidence: `C:/Users/suppo/.codex/memories/ai-tools-self-audit-phase04/REVIEW-SUMMARY.json`
- Upstream gate-review guidance: `.external/ai-workspace-kit/TOOLING-PLAYBOOK.md`
- Local gate registry: `.planning/gates/registry.json`
</context>

<must_haves>
- GATE-03: release gate review uses current `ai-workspace-kit` gate-review guidance when available, with manual review and cross-repo fallback when no runnable capability exists.
- DOC-04: release definition of done is filled with real validation evidence.
- Self-use output is written outside the target project.
- Self-audit findings become release evidence, not automatic decisions.
- Default self-audit should not be dominated by historical phase artifacts, fixture input docs, generated packet output, or optional `.external` references.
- No automatic cross-repo indexer, mechanical gate-linter, or dependency is added.
- `XREPO-VALIDATOR-01` remains the deferred v2 prerequisite before automatic cross-repo validation.
- `GATELINT-01` remains evidence-only future work.
</must_haves>

<threat_model>
- **T-05-02-01 Self-audit remains too noisy (high):** Release evidence could be unusable if historical planning artifacts dominate findings.
  - **Mitigation:** tighten default source-document selection and add regression tests.
  - **Verification:** self-audit summary after hardening has materially fewer irrelevant findings, or release docs record explicit caveat.
- **T-05-02-02 Hidden automation creep (high):** Gate review could become an automatic kit or AI Tools runner.
  - **Mitigation:** manual review only; record future mechanical checks as evidence-only v2.
  - **Verification:** docs and changelog state no auto-run/dependency.
- **T-05-02-03 Over-filtering target evidence (medium):** Noise reduction could hide real target-project drift.
  - **Mitigation:** keep broad file discovery, but scope only source documents used for contract reference extraction.
  - **Verification:** existing drift fixtures for stale source layer and missing commands still pass.
</threat_model>

<tasks>
<task id="T1" type="modify">
<files>
- `tools/contract-drift-auditor/discovery.js`
- `tools/contract-drift-auditor/references.js`
- `test/contract-drift-auditor/discovery.test.js`
</files>
<read_first>
- `tools/contract-drift-auditor/discovery.js`
- `tools/contract-drift-auditor/checks.js`
- `shared/file-walker.js`
- `shared/ignore-policy.js`
- `test/contract-drift-auditor/discovery.test.js`
</read_first>
<action>
Harden default source-document selection for contract reference extraction.
Keep full file discovery intact, but avoid treating historical planning output
and fixture input docs as current project contracts by default.

Recommended approach:

- Continue discovering files for evidence and package scripts.
- Treat `AGENTS.md`, `CLAUDE.md`, local skill files, `.planning/PROJECT.md`,
  `.planning/STATE.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`,
  `.planning/gates/**`, and `.planning/cross-repo/**` as current contract
  source documents.
- Treat the active phase context/research/plans as source documents only when
  needed by the current run, or leave historical `.planning/phases/**` out of
  default source docs.
- Keep `.external/ai-workspace-kit` ignored as a nested checkout.
- Do not read secret-like file contents.

If an implementation option would require a larger CLI option design, keep the
change smaller and document the remaining caveat in `docs/RELEASE-READINESS.md`.
</action>
<verify>
`npm.cmd test -- test/contract-drift-auditor/discovery.test.js`
</verify>
<acceptance_criteria>
- Existing mature/missing-command/stale-source fixtures still discover required current evidence.
- Historical `.planning/phases/**` files are not default contract source documents for self-audit reference extraction.
- `.external/ai-workspace-kit` remains ignored.
- Discovery remains deterministic and path-normalized.
</acceptance_criteria>
</task>

<task id="T2" type="modify">
<files>
- `test/contract-drift-auditor/integration.test.js`
</files>
<read_first>
- `test/contract-drift-auditor/integration.test.js`
- `test/shared/fixture-helpers.js`
- `tools/contract-drift-auditor/index.js`
</read_first>
<action>
Add or update integration coverage proving the hardened default source
selection does not break required drift detection and does not mutate targets.
Prefer a focused fixture or temporary test project with:

- current `AGENTS.md`;
- historical `.planning/phases/01-old/01-PLAN.md` referencing a missing path;
- current source-layer reference that should still be checked;
- optional nested `.external/ai-workspace-kit` marker that should be ignored.
</action>
<verify>
`npm.cmd test -- test/contract-drift-auditor/integration.test.js`
</verify>
<acceptance_criteria>
- Findings do not include missing references sourced only from historical phase artifacts.
- Findings still include missing references from current `AGENTS.md` or current source-layer docs.
- Target tree hash remains unchanged.
</acceptance_criteria>
</task>

<task id="T3" type="verify">
<files>
- `docs/RELEASE-READINESS.md`
</files>
<read_first>
- `docs/RELEASE-READINESS.md`
- `.external/ai-workspace-kit/TOOLING-PLAYBOOK.md`
- `.external/ai-workspace-kit/templates/GATE-REGISTRY.json`
- `.planning/gates/registry.json`
- `AGENTS.md`
- `.planning/cross-repo/CROSS-REPO-CAPABILITY-REQUESTS.md`
</read_first>
<action>
Perform manual release gate review and record the result in
`docs/RELEASE-READINESS.md`.

Review:

- gate relevance to stages;
- stale assumptions;
- duplication with kit-owned responsibilities;
- duplication with future AI Tools mechanical gate-linter;
- hidden automation or dependency creep;
- skip/blocking coherence;
- observable artifact output.

Use current `ai-workspace-kit` guidance as semantic input. Do not run or invent
an upstream command. If a conflict requires kit-owned change, create a
cross-repo outbox request or record a decision instead of implementing it.
</action>
<verify>
`Select-String -Path docs/RELEASE-READINESS.md -Pattern "Gate Review","assistant-led","evidence only","hidden automation","dependency"`
</verify>
<acceptance_criteria>
- Release readiness records GATE-03 evidence.
- Review outcome is adopt/revise/defer/reject/request-capability where relevant.
- Mechanical gate-linter remains evidence-only future work.
- No automatic tool run or dependency is introduced.
</acceptance_criteria>
</task>

<task id="T4" type="verify">
<files>
- `docs/RELEASE-READINESS.md`
- `CHANGELOG.md`
</files>
<read_first>
- `package.json`
- `tools/contract-drift-auditor/README.md`
- `docs/RELEASE-READINESS.md`
</read_first>
<action>
Run release validation:

1. `npm.cmd test -- test/contract-drift-auditor/discovery.test.js`
2. `npm.cmd test -- test/contract-drift-auditor/integration.test.js`
3. `npm.cmd test -- test/planning/release-docs.test.js`
4. `npm.cmd test`
5. Run self-audit with external output:
   `node tools/contract-drift-auditor/cli.js --project . --out C:\Users\suppo\.codex\memories\ai-tools-self-audit-phase05`

Record command, output path, packet status, important finding count, and
interpretation in `docs/RELEASE-READINESS.md`. If the packet still has noisy
findings, record the caveat and whether it blocks the first release.
</action>
<verify>
`npm.cmd test`
</verify>
<acceptance_criteria>
- Full tests pass.
- Self-audit output is outside the target project.
- Release readiness references the self-audit packet location and summary.
- Release readiness says whether remaining findings are blockers, human-review evidence, or caveats.
</acceptance_criteria>
</task>

<task id="T5" type="modify">
<files>
- `CHANGELOG.md`
</files>
<read_first>
- `CHANGELOG.md`
- `docs/RELEASE-READINESS.md`
- `.planning/research/AI-WORKSPACE-KIT-UPSTREAM-REVIEW.md`
</read_first>
<action>
Update the Phase 05 changelog entry with:

- changed auditor filtering behavior, if implemented;
- release readiness evidence;
- validation run;
- self-use impact;
- gate review result;
- compatibility impact for `ai-workspace-kit`;
- breaking changes and migration notes.
</action>
<verify>
`Select-String -Path CHANGELOG.md -Pattern "Phase 05","self-use","gate review","2079ab9","XREPO-VALIDATOR-01"`
</verify>
<acceptance_criteria>
- Changelog is concise, non-marketing, and useful as downstream freshness evidence.
- It explicitly states no automatic kit dependency, automatic cross-repo validation, or gate-linter automation was added.
</acceptance_criteria>
</task>
</tasks>

<verification>
- `npm.cmd test -- test/contract-drift-auditor/discovery.test.js`
- `npm.cmd test -- test/contract-drift-auditor/integration.test.js`
- `npm.cmd test -- test/planning/release-docs.test.js`
- `npm.cmd test`
- `node tools/contract-drift-auditor/cli.js --project . --out C:\Users\suppo\.codex\memories\ai-tools-self-audit-phase05`
- Search release docs and changelog for `ai-workspace-kit`, `optional`, `evidence only`, `XREPO-VALIDATOR-01`, and `GATELINT-01`.
</verification>

<success_criteria>
- Self-audit evidence is usable enough for first-release readiness or its limitations are explicitly recorded.
- Manual gate review satisfies GATE-03 without hidden automation.
- Release readiness is filled with actual validation and self-use evidence.
- Existing auditor tests still pass, proving filtering did not hide required fixture drift.
- Deferred cross-repo validator and mechanical gate-linter remain explicit v2 work.
</success_criteria>
