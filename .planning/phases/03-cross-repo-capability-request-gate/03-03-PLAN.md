---
phase: 3
plan: "03-03"
type: implementation
wave: 3
depends_on:
  - "03-02"
files_modified:
  - .planning/cross-repo/inbox/REQ-20260507-ai-workspace-kit-to-ai-tools-contract-drift-auditor.md
  - .planning/cross-repo/inbox/REQ-20260507-ai-workspace-kit-to-ai-tools-changelog-gate-review.md
  - .planning/cross-repo/outbox/REQ-20260507-ai-tools-to-ai-workspace-kit-review-packet-contract.md
  - .planning/cross-repo/decisions/DEC-REQ-20260507-ai-workspace-kit-to-ai-tools-changelog-gate-review.md
  - test/planning/cross-repo-protocol.test.js
  - CHANGELOG.md
  - .planning/todos/pending/2026-05-07-accept-gate-linter-request.md
autonomous: true
requirements:
  - XREPO-01
  - XREPO-02
  - XREPO-03
  - XREPO-04
  - XREPO-05
  - XREPO-06
  - XREPO-07
  - XREPO-08
  - GATE-01
  - GATE-02
  - GATE-04
  - GATE-05
  - GATE-06
---

# Plan 03-03: Add Example Requests and Docs Validation

## Gate Resolution

- **Research Gate:** resolved as `Research first`; user selected `1`; evidence in `03-RESEARCH.md`.
- **Upstream Freshness Gate:** checked before planning; local and remote `ai-workspace-kit` commits match `48ec037d058747151c320ced9c0ee1e1d247d4b1`; no upstream changelog exists at that commit.
- **Cross-Repo Incoming Review Gate:** applies and is executed by converting the real pending `ai-workspace-kit` request into inbox plus decision artifacts.
- **Cross-Repo Outgoing Need Gate:** applies and is executed by creating an outbox request to `ai-workspace-kit` for stable review packet schema/evidence-ref contract semantics.
- **Project Changelog Gate:** applies because Phase 3 changes workflow gates; update `CHANGELOG.md` before final verification.
- **AI Tools Self-Use Gate:** no validated AI Tools self-auditor exists yet; skip recorded in `03-RESEARCH.md`.
- **New Tool Intake Gate:** future mechanical `gate-linter` remains deferred as v2 evidence-only support.

<objective>
Populate the cross-repo protocol with the required bidirectional example
requests, convert the real incoming `ai-workspace-kit` request into a structured
request and decision, and add docs validation proving the protocol and gate
registry contain required fields.
</objective>

<context>
- Depends on plan `03-01` protocol structure and registry.
- Depends on plan `03-02` playbook and AGENTS routing.
- Pending request: `.planning/todos/pending/2026-05-07-accept-gate-linter-request.md`
- Existing test style: `test/review-packet/schema-validation.test.js`
- Package script: `npm test` runs `node --test`
</context>

<must_haves>
- D-24: Convert the pending todo into a structured inbox request from `ai-workspace-kit` to AI Tools.
- D-25: The decision accepts changelog convention and semantic gate review, plans Phase 3 registry/validation work, and defers mechanical `gate-linter`.
- D-26: The decision status is `planned`.
- D-27: Do not create a new phase from the incoming request.
- D-28: Add example request from `ai-workspace-kit` to AI Tools for a read-only contract drift auditor.
- D-29: Add the real incoming changelog/gate-review/gate-linter request, not only synthetic examples.
- D-30: Examples clearly prevent copied `.planning`, auto-runs, dependencies, or duplicate ownership.
- D-31: Add Node `node:test` docs validation for directories, template headings, vocabularies, examples, and registry shape.
- D-32: Validation checks Phase 3 context contains `Gate Resolution` with Trusted self-questioning evidence.
- D-33: Tests remain mechanical and do not decide semantic gate relevance.
- XREPO-08: Examples and validation prove both directions without repo dependency or automatic integration.
- GATE-01: `CHANGELOG.md` is updated for Phase 3 workflow gate changes and validation.
</must_haves>

<threat_model>
- **T-03-03-01 Examples become hidden commitments (medium):** Example requests could be mistaken for automatic roadmap work.
  - **Mitigation:** Set status to `proposed`, include clear non-goals, and explain that decisions are required before commitment.
  - **Verification:** Docs validation checks request status and non-goal sections.
- **T-03-03-02 Real request remains duplicated in todo form (low):** Keeping the pending todo after conversion can confuse future agents.
  - **Mitigation:** Delete the pending todo only after inbox request and decision artifacts exist.
  - **Verification:** Check git diff shows the structured request and decision before removing the todo.
- **T-03-03-03 Validation overreaches into semantic review (medium):** Tests could hard-code assistant judgment instead of structure.
  - **Mitigation:** Limit tests to file existence, headings, allowed vocabulary, registry fields, and required phrases.
  - **Verification:** Test names and assertions are mechanical; semantic outcomes remain in decision docs.
</threat_model>

<tasks>
<task id="T1" type="create">
<files>
- `.planning/cross-repo/inbox/REQ-20260507-ai-workspace-kit-to-ai-tools-contract-drift-auditor.md`
- `.planning/cross-repo/outbox/REQ-20260507-ai-tools-to-ai-workspace-kit-review-packet-contract.md`
</files>
<action>
Add the two required bidirectional example requests using
`CAPABILITY-REQUEST.md`:

1. From `ai-workspace-kit` to AI Tools:
   - Need: read-only `contract-drift-auditor`.
   - Boundary classification: `external ai-tools capability`.
   - Expected outputs: `REVIEW-SUMMARY.json`, `FINDINGS.md`, and `EVIDENCE.json`.
   - Non-goals: no adoption/bootstrap implementation inside AI Tools and no automatic kit dependency.

2. From AI Tools to `ai-workspace-kit`:
   - Need: stable review packet schema and evidence refs contract.
   - Boundary classification: `interop contract`.
   - Expected outputs: stable `REVIEW-SUMMARY.json` compatibility notes, evidence ref semantics, and severity/status mapping.
   - Non-goals: no dependency, no automatic tool execution, and no copied `.planning`.
</action>
<verify>
PowerShell:
`Select-String -Path .planning/cross-repo/inbox/REQ-20260507-ai-workspace-kit-to-ai-tools-contract-drift-auditor.md,.planning/cross-repo/outbox/REQ-20260507-ai-tools-to-ai-workspace-kit-review-packet-contract.md -Pattern "REVIEW-SUMMARY.json","Boundary classification","## Non-Goals"`
</verify>
<acceptance_criteria>
The required example requests exist in both directions and demonstrate the
ownership boundary without creating work automatically.
</acceptance_criteria>
</task>

<task id="T2" type="create">
<files>
- `.planning/cross-repo/inbox/REQ-20260507-ai-workspace-kit-to-ai-tools-changelog-gate-review.md`
- `.planning/cross-repo/decisions/DEC-REQ-20260507-ai-workspace-kit-to-ai-tools-changelog-gate-review.md`
- `.planning/todos/pending/2026-05-07-accept-gate-linter-request.md`
</files>
<action>
Convert the pending todo into the first real structured incoming request and
decision.

The request must capture:
- changelog gate requirement;
- freshness gate compatibility order;
- assistant-owned semantic gate review;
- future mechanical gate-linter support as evidence-only;
- non-goals around dependency, auto-run, auto-phase creation, and duplicated
  contract drift auditing in `ai-workspace-kit`.

The decision must:
- use `Decision: planned`;
- accept changelog convention as a process requirement;
- accept assistant-owned semantic gate review as a process requirement;
- mark Phase 3 registry and docs validation as planned/current scope;
- defer mechanical `gate-linter` as v2 optional AI Tools capability;
- state that tool output is evidence, not final decision.

After both artifacts exist and validate mechanically, remove the pending todo
so the same request is not processed twice.
</action>
<verify>
PowerShell:
`Select-String -Path .planning/cross-repo/decisions/DEC-REQ-20260507-ai-workspace-kit-to-ai-tools-changelog-gate-review.md -Pattern "Decision: planned","Scope Accepted","Scope Rejected","evidence"`
</verify>
<acceptance_criteria>
The real incoming request is represented as cross-repo protocol evidence and no
longer exists only as an unstructured pending todo.
</acceptance_criteria>
</task>

<task id="T3" type="create">
<files>
- `test/planning/cross-repo-protocol.test.js`
</files>
<action>
Add Node `node:test` docs validation that checks:

- `.planning/cross-repo/inbox`, `outbox`, `decisions`, and `templates` exist;
- request template includes required top fields and headings;
- decision template includes required fields/headings and allowed statuses;
- allowed boundary classifications are present;
- playbook includes Outgoing Need Gate, Incoming Review Gate, ownership boundaries, and no-automatic-work language;
- `.planning/gates/registry.json` parses and includes required gate IDs and required fields;
- example requests and real incoming request include required fields, expected outputs, and non-goals;
- Phase 3 context contains `Gate Resolution` and Trusted self-questioning evidence;
- Phase 3 research and plan files contain `Gate Resolution` sections.

Keep assertions mechanical. Do not assert whether a request should be accepted
or rejected beyond checking the recorded decision fields.
</action>
<verify>
PowerShell:
`npm.cmd test -- test/planning/cross-repo-protocol.test.js`
</verify>
<acceptance_criteria>
Docs validation fails if the protocol, templates, examples, registry, or gate
evidence structure is missing or incomplete.
</acceptance_criteria>
</task>

<task id="T4" type="update">
<files>
- `CHANGELOG.md`
</files>
<action>
Update `CHANGELOG.md` for Phase 3 implementation. Include:

- changed cross-repo protocol artifacts;
- changed gates and registry;
- changed schemas/templates if any;
- changed review packet/interoperability semantics;
- compatibility impact for `ai-workspace-kit`;
- breaking or potentially breaking changes;
- migration notes;
- validation run;
- upstream impact, including the `48ec037` commit and absence of upstream changelog.
</action>
<verify>
PowerShell:
`Select-String -Path CHANGELOG.md -Pattern "Phase 03","cross-repo","registry","48ec037"`
</verify>
<acceptance_criteria>
Downstream planning can read the changelog first and understand what Phase 3
changed without parsing every artifact.
</acceptance_criteria>
</task>

<task id="T5" type="verify">
<files>
- `.planning/cross-repo/**`
- `.planning/gates/registry.json`
- `test/planning/cross-repo-protocol.test.js`
- `CHANGELOG.md`
</files>
<action>
Run focused docs validation and full regression:

- `npm.cmd test -- test/planning/cross-repo-protocol.test.js`
- `npm.cmd test`

Then inspect for boundary regressions:
- no imports from `.external/ai-workspace-kit`;
- no automatic tool-running commands added;
- no package dependency added;
- no new roadmap phase created from the incoming request.
</action>
<verify>
PowerShell:
`npm.cmd test`
</verify>
<acceptance_criteria>
All tests pass and Phase 3 remains a protocol/gate-enforcement phase, not a
tool implementation or cross-repo automation layer.
</acceptance_criteria>
</task>
</tasks>

<verification>
- Run `npm.cmd test -- test/planning/cross-repo-protocol.test.js`.
- Run `npm.cmd test`.
- Confirm all Phase 3 requirements are covered by plan frontmatter.
- Confirm every D-01 through D-33 context decision is referenced by at least one plan.
- Confirm the pending todo is removed only after the structured request and decision exist.
- Confirm `CHANGELOG.md` records validation and upstream impact.
</verification>

<success_criteria>
- Required example requests exist in both directions.
- The real incoming `ai-workspace-kit` request is captured as structured inbox and decision artifacts.
- Docs validation mechanically proves templates, registry, examples, and gate evidence exist.
- Changelog explains Phase 3 changes and compatibility impact.
- Phase 3 is ready for execution without automatic cross-repo integration or duplicated ownership.
</success_criteria>
