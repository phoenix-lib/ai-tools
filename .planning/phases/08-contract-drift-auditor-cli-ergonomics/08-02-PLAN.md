---
phase: 8
plan: "08-02"
type: implementation
wave: 2
depends_on:
  - "08-01"
files_modified:
  - tools/contract-drift-auditor/README.md
  - README.md
  - docs/RELEASE-READINESS.md
  - CHANGELOG.md
  - test/planning/release-docs.test.js
autonomous: true
requirements:
  - CLI-01
  - CLI-02
---

# Plan 08-02: CLI Documentation, Self-Use Evidence, and Release Validation

## Gate Resolution

- **Research Gate:** user selected `Research first`; evidence in `08-RESEARCH.md`.
- **Upstream Freshness Gate:** resolved. Updated kit interop docs and static `ai-tools` packet fixture confirm packet artifacts remain the compatibility contract.
- **AI Tools Self-Use Gate:** applies. Run `contract-drift-auditor` against this repo using the new CLI options with output outside the repo; output is evidence only.
- **Project Changelog Gate:** applies. Update `CHANGELOG.md` with changed CLI behavior, validation, compatibility impact, breaking changes, and migration notes.
- **Cross-Repo Outgoing Need Gate:** skipped with reason. Docs consume existing kit interop guidance; no new kit-owned capability is needed.
- **Future Gate Review Hook:** not applicable. This plan does not change gate semantics or implement gate-linter behavior.

<objective>
Document the new `contract-drift-auditor` CLI behavior, validate that release guidance and downstream compatibility remain accurate, and record self-use evidence for the new stdout/fail-on modes.
</objective>

<context>
- Depends on `08-01`.
- Phase context: `.planning/phases/08-contract-drift-auditor-cli-ergonomics/08-CONTEXT.md`
- Research: `.planning/phases/08-contract-drift-auditor-cli-ergonomics/08-RESEARCH.md`
- Auditor docs: `tools/contract-drift-auditor/README.md`
- Root docs: `README.md`
- Release readiness: `docs/RELEASE-READINESS.md`
- Changelog: `CHANGELOG.md`
- Docs tests: `test/planning/release-docs.test.js`
- Upstream kit interop: `.external/ai-workspace-kit/AI-TOOLS-INTEROP.md`
- Upstream static fixture: `.external/ai-workspace-kit/examples/ai-tools-review-packet/REVIEW-SUMMARY.json`
</context>

<must_haves>
- CLI-01: docs explain stable machine stdout mode and keep packet files as source of truth.
- CLI-02: docs explain quiet mode, fail-on policy, and stable exit codes.
- D-08: update `tools/contract-drift-auditor/README.md`, root `README.md` where useful, and release docs.
- Changelog gate: changed scope, validation, upstream impact, compatibility impact, breaking changes, and migration notes.
- Self-use gate: run applicable validated tool as read-only evidence or record skip reason.
- Do not imply a published npm package or global install path.
- Do not apply new CLI flags to `cross-repo-compatibility-checker` in this phase.
</must_haves>

<threat_model>
- **T-08-02-01 Docs overpromise CI decisions (high):** Docs could imply `--fail-on` is a semantic approval/rejection engine.
  - **Mitigation:** explicitly state fail policy is caller-selected shell policy; packet findings remain evidence.
  - **Verification:** docs tests/search for source-of-truth and evidence-only wording.
- **T-08-02-02 Compatibility drift with kit fixture (medium):** Docs could describe stdout JSON as a new packet artifact.
  - **Mitigation:** keep required artifacts unchanged and mention stdout is a projection.
  - **Verification:** release docs and README still list the same four required artifacts.
- **T-08-02-03 Non-portable self-use evidence (medium):** Self-audit docs could encode a machine-local path as reusable command.
  - **Mitigation:** reusable docs use `<external-dir>`; actual local path recorded only as historical evidence.
  - **Verification:** release docs test continues rejecting machine-local reusable command.
- **T-08-02-04 Missing changelog evidence (medium):** Downstream freshness checks would miss CLI behavior changes.
  - **Mitigation:** add a concise Phase 08 changelog entry.
  - **Verification:** `Select-String` for Phase 08, `--format json`, `--quiet`, `--fail-on`, and `ai-workspace-kit`.
</threat_model>

<tasks>
<task id="T1" type="modify">
<files>
- `tools/contract-drift-auditor/README.md`
- `README.md`
</files>
<read_first>
- `tools/contract-drift-auditor/README.md`
- `README.md`
- `.planning/phases/08-contract-drift-auditor-cli-ergonomics/08-CONTEXT.md`
</read_first>
<action>
Document the CLI flags:

- `--format json`;
- `--quiet`;
- `--fail-on blocked|human_review_required|never`;
- exit codes `0`, `1`, and `2`;
- examples for CI/assistant usage.

Keep root README concise. Put detailed semantics in the auditor README.
</action>
<verify>
`Select-String -Path tools/contract-drift-auditor/README.md,README.md -Pattern "--format json","--quiet","--fail-on","REVIEW-SUMMARY.json"`
</verify>
<acceptance_criteria>
- Auditor README explains stdout projection versus packet source of truth.
- Root README remains a short entrypoint and does not duplicate every detail.
- Docs do not imply a published npm package or global install.
</acceptance_criteria>
</task>

<task id="T2" type="modify">
<files>
- `docs/RELEASE-READINESS.md`
- `test/planning/release-docs.test.js`
</files>
<read_first>
- `docs/RELEASE-READINESS.md`
- `test/planning/release-docs.test.js`
- `.external/ai-workspace-kit/AI-TOOLS-INTEROP.md`
</read_first>
<action>
Update release readiness and docs validation:

- record Phase 8 CLI ergonomics as post-v1 hardening;
- document reusable self-audit command with `<external-dir>`;
- mention machine stdout and fail-on policy are optional caller conveniences;
- ensure docs validation checks required CLI behavior and preserves portable self-audit guidance.
</action>
<verify>
`npm.cmd test -- test/planning/release-docs.test.js`
</verify>
<acceptance_criteria>
- Release docs say packet files remain source of truth.
- Reusable command does not contain `C:\Users`.
- Docs tests cover the new CLI flags or exit-code language where practical.
</acceptance_criteria>
</task>

<task id="T3" type="modify">
<files>
- `CHANGELOG.md`
</files>
<read_first>
- `CHANGELOG.md`
- `.planning/research/AI-WORKSPACE-KIT-UPSTREAM-REVIEW.md`
</read_first>
<action>
Add a Phase 08 changelog entry covering:

- changed scope: CLI stdout/quiet/fail-on behavior;
- validation commands;
- upstream `ai-workspace-kit` impact;
- compatibility impact;
- breaking changes: none by default because `--fail-on never`;
- migration notes: CI can opt into `--fail-on`.
</action>
<verify>
`Select-String -Path CHANGELOG.md -Pattern "Phase 08","--format json","--quiet","--fail-on","ai-workspace-kit"`
</verify>
<acceptance_criteria>
- Changelog is useful to downstream freshness checks.
- Entry states packet schema/artifact semantics did not change.
</acceptance_criteria>
</task>

<task id="T4" type="verify">
<files>
- `tools/contract-drift-auditor/README.md`
- `docs/RELEASE-READINESS.md`
- `CHANGELOG.md`
</files>
<read_first>
- `tools/contract-drift-auditor/cli.js`
- `.planning/phases/08-contract-drift-auditor-cli-ergonomics/08-RESEARCH.md`
</read_first>
<action>
Run self-use and final validation:

1. Run focused CLI and docs tests.
2. Run full `npm.cmd test`.
3. Run `contract-drift-auditor` against this repository with:
   - an external `--out`;
   - `--format json`;
   - `--fail-on never`.
4. Optionally run a focused command with `--quiet` to prove success chatter is suppressed.
5. Record command, output path, packet status, finding count, and caveats in release readiness or Phase 8 summary material during execution.
</action>
<verify>
`npm.cmd test`
</verify>
<acceptance_criteria>
- Full tests pass.
- Self-use output path is outside the repository.
- Self-use JSON stdout parses and matches generated packet status.
- Documentation and changelog reflect the implemented behavior.
</acceptance_criteria>
</task>
</tasks>

<verification>
- `npm.cmd test -- test/contract-drift-auditor/cli.test.js test/planning/release-docs.test.js`
- `npm.cmd test`
- `node tools/contract-drift-auditor/cli.js --project . --out <external-dir> --format json --fail-on never`
- `node tools/contract-drift-auditor/cli.js --project . --out <external-dir> --quiet`
- `Select-String -Path tools/contract-drift-auditor/README.md,README.md,docs/RELEASE-READINESS.md,CHANGELOG.md -Pattern "--format json","--quiet","--fail-on","REVIEW-SUMMARY.json","evidence"`
</verification>

<success_criteria>
- CLI behavior is documented for humans, CI, and assistants.
- Changelog captures compatibility impact for `ai-workspace-kit` freshness checks.
- Release guidance remains portable and evidence-only.
- Self-use evidence confirms the new CLI modes work on this repository.
</success_criteria>

